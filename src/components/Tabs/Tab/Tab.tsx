import { ReactNode } from 'react';

export interface ITabProps {
  children?: ReactNode;
  label: string;
  id: string;
  isSelected?: boolean;
}

export function Tab (props: ITabProps) {
  return null;
}
